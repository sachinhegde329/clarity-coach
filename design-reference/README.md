# Design reference assets

This folder holds Stitch HTML→RN conversions and other design-time artifacts. They are **not** imported by the mobile app.

Regenerate with:

```bash
npm run stitch:convert-rn
npm run stitch:generated-index
```

Output is written to `design-reference/stitch-generated/` (gitignored).
